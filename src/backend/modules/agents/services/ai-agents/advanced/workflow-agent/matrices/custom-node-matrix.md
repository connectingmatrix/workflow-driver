# Custom Node Matrix

- x12-837-parser: No discovered first-party X12 parser node.
- x12-835-parser: No discovered first-party X12 parser node.
- phi-redaction-gate: Used only when existing nodes cannot enforce this contract.
- data-quality-gate: Used only when existing nodes cannot enforce this contract.
- model-evaluation-gate: Used only when existing nodes cannot enforce this contract.
- ai-workflow-governor: Used only when existing nodes cannot enforce this contract.
- claim-denial-labeler: Used only when existing nodes cannot enforce this contract.
- model-drift-monitor: Used only when existing nodes cannot enforce this contract.
