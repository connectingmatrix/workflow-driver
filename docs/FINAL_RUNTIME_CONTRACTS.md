# @connectingmatrix/workflow-driver final runtime contracts

This file is generated by the final process-monitor / project / nodes / workflow-AI / Giga-agents hardening pass.

## Ownership

- workflow CRUD/dataloader/graphql
- workflow designer launcher
- workflow execution ownership over backend workflow pieces
- workflow executions and process tracking
- workflow AI binding to preserved workflow-ai contract
- import of .node packages from @connectingmatrix/nodes

## Public API contracts

- `Workflows.getList(options)`
- `Workflows.getObject(id)`
- `Workflows.search(term)`
- `Workflows.create(input)`
- `Workflows.update(id,patch)`
- `Workflows.delete(id)`
- `Workflows.validate(workflow)`
- `Workflows.execute(id,input,options)`
- `Workflows.abort(processId,reason)`
- `Workflows.versions(id)`
- `Workflows.versions.delete(workflowId,versionId)`
- `Workflows.versions.publish(workflowId,versionId)`
- `Workflows.executions.list(filter)`
- `Workflows.onWorkflowExecute(handler)`
- `Workflows.onWorkflowCatalog(handler)`
- `Workflows.bindWorkflowAgent(agent)`
- `Workflows.startWorkflowAISession(workflowId,options)`
- `Workflows.workflowAIMessage(sessionId,message)`
- `Workflows.debugWithAI(workflowId,input)`
- `Workflows.importNodePackage(workflowId,nodePackage)`

## Routes / launcher

- `GET /workflows/health`
- `POST /workflows/execute`
- `POST /workflows/validate`
- `POST /workflows/ai/debug`
- `GET /workflows/launcher`

## Notes

- Imports preserved workflow/workflow-executor packages by contract; it does not rewrite those packages.

## Stub launcher

Run the local package launcher with:

```bash
npm run build
node playground.mjs
```
