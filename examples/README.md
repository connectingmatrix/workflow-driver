# @connectingmatrix/workflow-driver examples

These files are for package launch/debug/testing only. They are not the production Giga UI or backend.

Use them to verify package health/status, local examples, and package-owned contracts while the real app consumes this package through a `file:` dependency.
